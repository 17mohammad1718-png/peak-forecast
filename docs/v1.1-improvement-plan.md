# پلن اصلاح v1.1 — سناریوی GPT-6 (2026-09-07)

منبع: docs/gpt6-consult-brief.txt → پاسخ ممیزی GPT-6 (کامل نگهداری در همین پوشه).
هر سه ادعای عددی GPT-6 با داده خودمان تأیید شد: ضریب 1.175→×1.2=3.384M (دقیق)،
137.79M÷35=3.937M (دقیق)، 1.8M×0.84=1.512M (دقیق). کلاس ابرپیک 6.7% (~5% مورد انتظار).

## حکم نهایی روی ۱۲ سوال

| # | حکم | تصمیم |
|---|---|---|
| 1 | پذیرفتن با اصلاح | کلاس‌ها **مطلق** (DI≥75 ابرپیک) + رتبه صدکی به عنوان badge جدا؛ گارد تعطیل پنجشنبه/جمعه → badge «تعطیل مهم» نه override کلاس. DI mapping فریز |
| 2 | پذیرفتن کامل | کف = فرمول مشارکت: `gross_floor(n) = (C_turn/n + C_night + contribution) / 0.84` — worksheet هزینه با خودمیزبان؛ تا آنجا 1.8M موقت. کف پس از همه تخفیف‌ها enforce شود؛ اگر سقف رقیب < کف اقتصادی → flag درfeasible |
| 3 | پذیرفتن کامل | pace فقط با **champion/challenger BSS≥5%** (paired bootstrap روی week-blocks) ارتقا بگیرد؛ R = residual از prior همان lead/تقویم نه ضریب خام؛ λ از فرم n_eff/(n_eff+k) واقعی |
| 4 | پذیرفتن با اصلاح | توپولوژی اول: gap یک‌روزه=پل قطعی، دو روزه=کاندید. اعلام رسمی: منابع دولتی (dolat.ir/ostan-mz) فاقد RSS معتبر — فعلاً فقط **anomaly رقبا** (≥10 رقیب معتبر، ≥30% مشارکت) به عنوان «فعالیت غیرعادی» نه «پیش‌بینی پل». مین‌استی از فیلدهای جمع‌شده نیست → build نشود |
| 5 | پذیرفتن کامل (اولویت ۱) | state جدا برای «تقویم باز نشده»؛ R=1 و سقف/کف رقیب غیرفعال روی unknown؛ fallback: log(P/A)=c·log(P_cal/A) با c بر اساس پوشش؛ far-out premium فقط روی «شب عادیِ با اطلاعات» — روی dead-zone تعلیق |
| 6 | پذیرفتن کامل | مجموعه 6-12 قابل مقایسه ثابت (فصلی/بازبینی)، مقایسه در lead یکسان 14-28 شب بدون R/premium، A_market=median-of-medians، دی‌بند ±7% ×2 هفته، حرکت نصف با سقف ±10%، 5 فیلد تصمیم در داشبورد |
| 7 | پذیرفتن با اصلاح | جدا کردن خطای پیش‌بینی از خطای سیاست قیمت؛ split observed/unknown؛ شکستن WAPE به lead-bucket/تعطیل/پوشش؛ ablation یک‌به‌یک قوانین (کدام bound واقعاً می‌بندد) قبل از هر tuning |
| 8 | پذیرفتن | state چهارگانه own: sellable/guest-booked/host-blocked/unknown؛ بلاک=هندسه عملیاتی (مرز gap یتیم)؛ هشدار «بلاک انعطاف‌پذیر را باز کن» فقط روی strong dates |
| 9 | پذیرفتن | tiers فعلی=prior؛ استخراج متن فقط **shadow mode** با سقف 10-20% اثر، تغییر سالانه ≤5 پوینت، اگر ordering بین قواعد متفاوت شد → دست نزن |
| 10 | پذیرفتن | عید‌ها=leisure window (ضریب کامل)، محرم عادی 1-8=خنثی (بدون تخفیف)، تسوعا/عاشورا=برجسته ولی مجزا از عید؛ residual های رویداد فقط با شواهد انباشته |
| 11 | پذیرفتن کامل | سه افزودنی: «تصمیم امروز» (اقدام+دلایل+ددلاین)، «اقتصاد پیشنهاد» (خالص پس از کمسیون+عبور از کف؟+hurdle نسبت q: q1/q0>(0.84T0-C)/(0.84T1-C))، «done/hold/reject» با ثبت پاسخ. قالب ≤12 خط |
| 12 | پذیرفتن (اولویت ۳) | **Ledger رزرو+تصمیم**: جداول reservation (id,dates,payout,cancel) + decision (ts,target,suggested,applied,accepted,reason). منبع: خروجی JSON رزروها + پاسخ تلگرام. ≤200 خط |

## ترتیب اجرا (v1.1)

1. **Unknown-horizon state + fallback pricing + تعلیق far-out روی dead-zone** (نصف روز)
2. **کف اقتصادی stay-length** (نصف روز + worksheet با کاربر)
3. **Ledger رزرو/تصمیم** (یک روز + backfill 35 رزرو)
4. **Pace challenger** (۱-۲ روز، shadow mode)
5. **تلگرام action-led** (نصف روز بعد از ledger)

## چیزهایی که عمداً نمی‌سازیم

- استخراج مدت اقامت فارسی در production (فقط shadow)
- تشخیص پل از منابع خبری (RSS معتبر نداریم؛ فقط anomaly رقبا)
- مین‌استی-انحصار detector (فیلد جمع نمی‌شود)
- هرگونه تخفیف/پرمیوم بدون دلیل صریح قابل نمایش در داشبورد
