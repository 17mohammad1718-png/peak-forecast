# موجودی داده‌های واقعی (بررسی‌شده 2026-09-07 — v2 بعد از تحقیق)

همه مسیرها همین لپ‌تاپ، همه از پروژه جاجیگا — بدون scraping جدید.

## ۰. دو گنج جدید (کشف 2026-09-07 عصر)
- **radar_history.db** — `jajiga-tracker/data/radar/history/` — ۲۹ اسنپ‌شات روزانه از
  2026-08-08؛ جدول days با `first_seen` (تاریخ اولین «پر» دیده‌شدن هر شب) =
  منحنی رزرو/pace واقعی. ۴۰۵۳ رکورد، ۳۵ اتاق، تا 2026-12-21.
- **corpus نظرات** — `jajiga-tracker/data/reviews_mining/corpus.json` — ۶۲,۲۹۳ نظر؛
  **۷,۶۴۶ نظر بابلکنار** از ۲۰۱۹ (۱۱۶ اقامتگاه) → فصلی‌بندی واقعی ۷ ساله برای نمای کل سال.
- **persian_holiday.db** — `peak-forecast/data/` — تعطیلات رسمی جلالی ۱۳۷۰→۱۴۲۰ (دانلودشده،
  تاییدشده: ۱۴۰۵ = ۳۴ تاریخ تعطیل).

## ۱. تقویم رقبای رادار — `H:/projects/jajiga-tracker/data/radar/`
- **35 فایل اتاق** + radar-config.json
- هر فایل: `room_id, meta{title,village,host,own}, fetched_at, nights[]`
- هر شب: `date, price, is_instant, is_weekend, is_unavailable` (+گاهی discount)
- افق تقویم: ~77 شب جلوتر (مثال 3149747: 2026-09-06 تا 2026-11-21)
- **نکته کلیدی:** این فایل‌ها لحظه‌ای بازنویسی می‌شوند (fetch_radar). تاریخچه «کِی پر شد»
  فقط از **snapshot روزانه** به دست می‌آید، نه این فایل‌ها.

## ۲. اسنپ‌شات‌های supply — `H:/projects/jajiga-tracker/data/snapshots/`
- **22 فایل** روزانه از `supply-2026-08-01.json` تا `supply-2026-09-07.json`
- محتوا: `date, meta_counts, room_ids[]` (شناسه اقامتگاه‌های فعال بابلکنار، ~500 تا)
- این‌ها لیست شناسه هستند، وضعیت شب‌به‌شب تقویم را ندارند → سیگنال «پر شدن تدریجی»
  هنوز ذخیره نمی‌شود؛ از امروز با کالکشن جدید این پروژه شروع می‌شود.

## ۳. داده قیمت ۱۰۰ کلبه — `H:/projects/jajiga-tracker/data/pricing/pricing-dataset.json`
- 104 ردیف (100 سالم) از ۴ روستا: سیدکلا، قرآن‌تالار، گونه‌کلا، شیردَرکلا
- فیلدها: `village, min_price, occupancy_30, pool, jacuzzi, own, ...`

## ۴. رزروهای خودم — chalet-dashboard v3
- مسیر: `H:/hermes outputs/chalet-project`
- 35 رزرو seed تا 1405/06/23؛ خالص 137,791,000 تومان؛ در localStorage مرورگر
  (خروجی JSON دستی، یا از دکمه «خروجی JSON» داشبورد)
- نرخ کمسیون تاییدشده: خودم 16%، رقبا 12%

## ۵. revenue realized — `H:/projects/jajiga-tracker/data/revenue/`
- `realized-seydkola-mordad-1405.json`: اشغال/درآمد واقعی 2026-08-08..09-06 از روی
  دیف snapshotها؛ فقط 1 شب پر برای خودم، رقبا بیشتر.
- ارزش: اولین داده «واقعیت در برابر پیش‌بینی» برای اعتبارسنجی.

## ۶. آنچه کم است (گپ‌ها)
1. **تاریخچه پر شدن شب‌ها** — از امروز شروع می‌کنیم؛ بعد از ~4 هفته سیگنال معنی‌دار می‌شود.
2. **تقویم تعطیلات رسمی جلالی ۱۴۰۵** — باید یک‌بار جمع شود (time.ir یا جدول دستی) → `data/holidays-1405.json`
3. **رزروهای خودم به فایل** — هنوز فقط در localStorage؛ نیاز به export دستی یک‌باره.
4. تاریخچه اشغال خودم قبل از 2026-08-08 وجود ندارد.

## جمع‌بندی برای الگوریتم
- **امروز قابل ساخت:** قانونی ساده روی تعطیلات/آخرهفته + قیمت‌های رقبا (سیگنال ۱،۲،۴)
- **بعد از ~۱ ماه کالکشن:** لایه occupancy رقبا (سیگنال ۳) فعال می‌شود
- **بعد از ~۶ ماه:** مدل آماری منطقی می‌شود
